export default interface WindowModel {
    id: any,
    title: string,
    zIndex: number,
    data?: any,
    width: number,
    height: number,
    x?: number | undefined,
    y?: number | undefined
}